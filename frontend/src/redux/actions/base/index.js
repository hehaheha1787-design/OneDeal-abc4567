// Base socket disabled for full on-chain version
const baseInit = async () => {
    // No socket needed - all data from chain
    console.log('Full on-chain mode - no backend socket');
};

export default baseInit;
