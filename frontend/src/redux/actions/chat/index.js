// Chat disabled for full on-chain version
const chatRoomConnect = async () => {
    // No chat socket needed
    console.log('Chat disabled - full on-chain mode');
};

export default chatRoomConnect;
